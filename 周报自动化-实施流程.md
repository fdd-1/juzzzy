# 周报自动化实施流程

> 目标：BI 报表导出 → 数据清洗整合 → 结论生成 → 格式化 Excel → 飞书电子表格嵌入文档
> 当前覆盖：4.1 服务指标跟进 & 语义分析（含 AI 学情助手）

---

## 一、整体流程（已跑通）

```
┌──────────────┐   ┌──────────┐   ┌──────────────┐   ┌──────────────┐   ┌─────────────────┐
│  BI 系统     │ → │ 多份原始 │ → │ 整合宽表     │ → │ 格式化 Excel │ → │ 飞书电子表格 +  │
│  (Smartbi)   │   │ Excel    │   │（含辅助列） │   │（多级表头） │   │ 文档嵌入        │
└──────────────┘   └──────────┘   └──────────────┘   └──────────────┘   └─────────────────┘
   bi_skill        exports/4_1/   consolidate_*.py    export_4_1_excel    create_feishu_sheets
                                  conclusions_*.py    + AI 单独成表       sheets +write 分批
                                                                          docs +update 嵌入
```

设计要点：
- **不向飞书文档写原生表格**：超过 ~200 行的原生表会让飞书文档卡顿，已弃用。
- **统一走「飞书电子表格 + 文档嵌入」**：文档里只放 `<sheet token="...">` 的引用块，加载即开即用，不卡。
- **本地一定先有格式化 Excel**：作为留档与人工核对依据；电子表格只是最终展示载体。
- **辅助列保留在整合宽表里**：跟进率、接通率、AI 占比等供结论引用，不进格式化 Excel。

---

## 二、目录结构

```
~/Desktop/周报自动化/
├── 周报自动化-实施流程.md        ← 本文档
├── weekly_report/
│   ├── consolidate_4_1_v2.py    ← 主表整合（6 份原始 → 一张宽表 + 辅助列）
│   ├── consolidate_4_1_ai.py    ← AI 学情单独成表 + 格式化 Excel
│   ├── conclusions_4_1.py       ← 结论文本生成（按团队挑差点 + LP 兜底）
│   ├── export_4_1_excel.py      ← 主表格式化 Excel（多级表头 / 汇总行加色）
│   ├── create_feishu_sheets.py  ← 创建飞书电子表格 + 嵌入文档
│   └── _archive/                ← 已弃用的脚本（仅留档）
└── exports/4_1/
    ├── 01_sop/ 02_lp_first_class/ 03_lp_first_specialty/
    ├── 04_first_call/ 06_lp_arch/ 07_ai_summary/  ← BI 原始导出
    ├── _merged_4_1.xlsx              ← 主表整合（含辅助列，结论用）
    ├── _merged_4_1_ai.xlsx           ← AI 学情整合
    ├── 4_1_格式化.xlsx                ← 主表格式化（最终上传源 1）
    └── 4_1_AI学情_格式化.xlsx          ← AI 表格式化（最终上传源 2）
```

---

## 三、阶段说明

### 阶段 1：BI 导出（沿用 bi_skill，无变更）

按 `_profiles_4_1.json` 列出的 6 份报表批量导出到 `exports/4_1/<编号>_<别名>/`：

| 编号 | 用途 |
|------|------|
| 01_sop | 海外思维服务 SOP 执行情况（首通 / 首课语义分析执行率） |
| 02_lp_first_class | 海外思维学管服务指标（-48h，首课） |
| 03_lp_first_specialty | 海外思维学管服务指标（-72h，首专） |
| 04_first_call | 益智海外新生首通监控 |
| 06_lp_arch | 海外思维 LP 架构表 |
| 07_ai_summary | AI 学情助手完成情况汇总 |

每份产出两类文件：原始 Excel + `_run.log`（导出过程日志）。

### 阶段 2：整合宽表（`consolidate_4_1_v2.py`）

**输入**：上述 6 份 Excel
**输出**：`exports/4_1/_merged_4_1.xlsx`

做的事：

1. 各源单独 loader（`load_sop` / `load_first_call` / `load_first_class` / `load_first_specialty` / `load_lp_arch` / `load_ai_summary`），统一字段名。
2. 团队级汇总：`海外团队（合）` + 9 个组（`TEAM_ORDER`），AI 占比按 sum/sum 重算，不取均值。
3. 个人级明细：以 02_lp_first_class 的 LP 列表为基准，依次 left join 其他 5 份。
4. 列序严格对齐周报图片版式（基础 → 首通语义分析 → 首通 → 首课 SOP → 首课 → 首专 → LP 架构）。
5. **辅助列追加在尾部**（`首通_跟进率`、`首通_接通率`、`首通_48小时企微绑定率`、`AI_首课/首专干预中占比`），仅给 conclusions 用，不进格式化 Excel。

⚠️ TEAM_ORDER 包含 `台湾组`，数据完整保留；台湾组只在结论环节排除。

### 阶段 3：AI 学情单独成表（`consolidate_4_1_ai.py`）

为什么单独成表：AI 学情字段口径与主表完全独立，强行并到主表会让列数膨胀且阅读困难。

**输出**：
- `_merged_4_1_ai.xlsx`：团队级（海外合 + 9 组）+ 个人级（按工龄倒序）。
- `4_1_AI学情_格式化.xlsx`：`基础 / 总览 / 首课AI干预 / 首专AI干预` 四组多级表头，干预中占比按 `0.0%` 渲染。

### 阶段 4：结论文本（`conclusions_4_1.py`）

读 `_merged_4_1.xlsx` 即可（辅助列就在尾部）。

约定：

- `EXCLUDE_TEAMS = {"台湾组"}` —— 台湾组数据保留但不进结论。
- `TARGETS` 月度目标：首通及时跟进 95%、首课及时跟进 85%、首专及时跟进 85%。
- 每个指标的写法：先报海外整体值 + 是否达目标 → 列出"低于整体值"的组，从低到高 → 每组兜 3 个最差 LP（阈值=本组指标值）。
- 输出纯文本（按缩进表示层级），由飞书 `callout` 块直接渲染。

### 阶段 5：格式化 Excel（`export_4_1_excel.py` + `consolidate_4_1_ai.py`）

主表格式化要点（`export_4_1_excel.py`）：

- 第 1 行分组标题（彩条 + 居中合并），第 2 行列名。
- 数值格式化：百分比 / 2 位小数 / 整数 / 日期分组定义在 `PCT_COLS` / `DEC2_COLS` / `INT_COLS`。
- 汇总行底色 `FFF2CC` + 加粗。
- `freeze_panes = "D3"` 冻结前两行 + 前 3 列。
- `~$_merged_4_1.xlsx` 是 Excel 临时锁文件，跑前确认主表 Excel 未被打开（否则 `pd.read_excel` 抢锁会报错）。

### 阶段 6：飞书电子表格 + 文档嵌入（`create_feishu_sheets.py`）

为什么不直接在飞书文档里建原生表格：
- 单文档块上限 ~2000 cell，分页拆表后又要管理多个块标题；
- 大表渲染极卡，团队反馈"打开就 lag"。

为什么不走「Excel 复制 → 飞书文档粘贴」：
- 飞书自研编辑器拦截了 `execCommand('insertHTML')` / `InputEvent('beforeinput')` / `navigator.clipboard.write` / OS 级 `Ctrl+V` 等所有程序化粘贴路径；
- 即使 DOM 看似插入了，飞书也不会触发自身的保存事件，刷新后丢失。

**当前方案**：每张展示表 = 一份独立的飞书电子表格 → 文档里只嵌入引用块。

```python
# 1) 建空表
lark-cli sheets +create --title "4.1 主表 - 服务指标+语义分析+LP架构"
# 返回 spreadsheet_token

# 2) 取 sheet_id
lark-cli sheets +info --spreadsheet-token <token>
# 返回 data.sheets.sheets[0].sheet_id   ← 注意嵌套两层

# 3) 分批写入（Windows 命令行 ≤8 KB，--values 不支持 @file）
#    batch_size=3~5 行，range = "<sheet_id>!A<start>:<endCol><end>"
lark-cli sheets +write --spreadsheet-token <token> --range <range> --values <inline JSON>

# 4) 文档里嵌入 sheet 块
lark-cli docs +update --api-version v2 \
  --doc <doc_id> --command append \
  --content '<h4>主表 - 服务指标 + 语义分析 + LP架构</h4><sheet token="..."></sheet>'
```

关键工程细节（曾经踩过的坑）：

- `sheets +create` 返回里 token 路径是 `data.spreadsheet_token`，**不是** `data.spreadsheet.spreadsheet_token`。
- `sheets +info` 返回里 sheet 列表是 `data.sheets.sheets[0].sheet_id`（多嵌套一层）。
- `--values` 不支持 `@file` 语法，必须 inline JSON。Windows 命令行 ≤8 KB，每批 3~5 行最稳。
- `docs +update --content @file` 可用，但路径**必须是相对路径** + 显式 `cwd=`。
- 文档落点用 ID 而非 URL：`ZFb3d1CZFobHnTxSSnMcgcVanyg`。

---

## 四、一次完整跑（4.1 服务）

```powershell
# 1) BI 导出（前置，按需）
python weekly_report\step1_export.py    # 沿用旧逻辑，未变

# 2) 整合 + 格式化
python weekly_report\consolidate_4_1_v2.py
python weekly_report\consolidate_4_1_ai.py
python weekly_report\export_4_1_excel.py

# 3) 结论文本（手动核对再贴）
python weekly_report\conclusions_4_1.py

# 4) 创建电子表格 + 嵌入文档
python weekly_report\create_feishu_sheets.py
```

跑完产出：

- 本地：`exports/4_1/4_1_格式化.xlsx`、`4_1_AI学情_格式化.xlsx`
- 飞书：两份独立电子表格 + 目标文档已嵌入两个 `<sheet>` 块

---

## 五、扩展到 4.2~4.7 的复用模板

每个子模块按同一套四件套落地：

| 文件 | 职责 |
|------|------|
| `consolidate_4_X.py` | 整合宽表 → `_merged_4_X.xlsx`，含辅助列 |
| `conclusions_4_X.py` | 读宽表生成结论纯文本，支持 `EXCLUDE_TEAMS` |
| `export_4_X_excel.py` | 多级表头 / 汇总行加色 / 数值格式化 |
| 复用 `create_feishu_sheets.py` | 抽公共函数：`create_sheet` / `write_data_in_batches` / `embed_sheet_in_doc`，每个子模块只填 Excel 路径 + 标题 |

> 整合规则随业务调整时改动只发生在 `consolidate_*.py`，渲染层和上传层稳定。

---

## 六、风险与规避

| 风险 | 规避 |
|------|------|
| 飞书 API 写入超 2000 cell 限制 | 单表拆表已不适用；改走电子表格嵌入后此限制不再触发 |
| Windows 命令行 ≤8 KB | `sheets +write` 强制 inline JSON 时 `batch_size=3~5` |
| `_merged_*.xlsx` 被 Excel 占用 | 跑脚本前确认关闭 Excel；`~$xxx.xlsx` 残留可清掉 |
| BI 字段漂移 | 各 loader 用列名定位（`首课_及时跟进率` 等），缺字段 fail-fast |
| 月度目标变化 | 改 `conclusions_*.py` 顶部 `TARGETS` |
| 自然周计算 | `week_dates.py` 输出 ISO 周（周一~周日） |

---

## 七、未覆盖的部分

- 4.2 ~ 4.7 子模块尚未落同一套脚本（按第五节模板补即可）。
- 多周对比 / 环比列暂未引入，结论里也未做"上周值"。如要加，建议在 `consolidate_*.py` 里多 load 一次上周快照，merge 后追加 `*_上周` 与 `*_环比` 列。
- 目前没有调度任务（cron / 计划任务），都是人工触发，便于核对。
